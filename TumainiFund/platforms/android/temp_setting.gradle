android {
            defaultConfig {
                applicationId = "org.nativescript.TumainiFundDev2"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }